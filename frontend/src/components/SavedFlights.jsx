import React, { useState } from "react";

export default function FlightSearch() {

    const API_URL = process.env.REACT_APP_API_URL;

    return (
        <div>
            <h2>Saved Flights List</h2>
        </div>
    );
}
